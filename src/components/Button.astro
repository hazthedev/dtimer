---
interface Props {
  href?: string;
  variant?: 'primary' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  class?: string;
  type?: 'button' | 'submit';
  target?: string;
  rel?: string;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  class: className = '',
  type = 'button',
  target,
  rel
} = Astro.props;

const baseStyles = 'inline-flex items-center justify-center font-body font-medium rounded-lg transition-colors transition-transform ease-out duration-300 hover:duration-200 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-sambal-red';

const variantStyles = {
  primary: 'bg-sambal-red text-bone-white hover:bg-charred-brown hover:shadow-lg hover:-translate-y-0.5 active:scale-[0.97] active:duration-100',
  outline: 'border-2 border-sambal-red text-sambal-red hover:bg-sambal-red hover:text-bone-white hover:-translate-y-0.5 active:scale-[0.97] active:duration-100',
  ghost: 'text-sambal-red hover:bg-sambal-red/10 hover:-translate-y-0.5 active:scale-[0.97] active:duration-100'
};

const sizeStyles = {
  sm: 'px-4 py-2 text-sm',
  md: 'px-6 py-3 text-base',
  lg: 'px-8 py-4 text-lg'
};

const combinedClasses = `${baseStyles} ${variantStyles[variant]} ${sizeStyles[size]} ${className}`;
---

{href ? (
  <a href={href} class={combinedClasses} target={target} rel={rel}>
    <slot />
  </a>
) : (
  <button type={type} class={combinedClasses}>
    <slot />
  </button>
)}
